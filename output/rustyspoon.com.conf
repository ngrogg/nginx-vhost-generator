# HTTP Section
server {
    ## Listen on HTTP port
    listen 80;
    listen [::]:80;
    
    ## Domain
    server_name rustyspoon.com www.rustyspoon.com;
    
    ## Redirect to HTTPS, use 302 for SEO
    return 302 https://$host$request_uri;
}
